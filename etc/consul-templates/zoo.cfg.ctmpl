tickTime=2000
initLimit=10
syncLimit=5
dataDir=/tmp/zookeeper/
clientPort=2181
{{$cluster := env "ZK_CLUSTER_NAME"}}{{$myip := env "ZK_ADDV_ADDR"}}{{$myid := env "MYID"}}{{if ne $myid ""}}{{range $tag, $services := service "zookeeper" "any"| byTag}}{{ if eq $tag $cluster }}{{range $i, $e := $services}}{{$idx := add $i 1}}server.{{$idx}}={{if eq $myip $e.Address}}0.0.0.0{{else}}{{$e.Address}}{{end}}:2888:3888
{{end}}{{end}}{{end}}{{end}}
